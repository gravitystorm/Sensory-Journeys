Installation Notes
==================

These are currently slightly cryptic, and are for Ubuntu 9.10

sudo apt-get install postgresql
sudo -u postgres -i

   11  createuser sensory -s -P
   12  createdb -E UTF8 -O sensory sensory_dev
   13  createdb -E UTF8 -O sensory sensory_test
   14  createdb -E UTF8 -O sensory sensory_prod

sudo apt-get install ruby1.8 rdoc1.8 ri1.8 ruby irb libxml2-dev ruby1.8-dev apache2-threaded-dev libmagick9-dev build-essential libopenssl-ruby apache2 libxml-ruby
wget http://rubyforge.org/frs/download.php/69365/rubygems-1.3.6.tgz
tar -xzvf rubygems-1.3.6.tgz 
cd rubygems-1.3.6/
sudo ruby setup.rb 
sudo ln -s /usr/bin/gem1.8 /usr/bin/gem

sudo gem install rails
sudo gem install pg
sudo gem install libxml-ruby

cd config
cp postgres.example.database.yml database.yml
nano database.yml
cp example.environment.rb environment.rb
nano environment.rb

rake db:migrate

sudo gem install passenger
sudo passenger-install-apache2-module  

sudo nano /etc/apache2/conf.d/passenger
LoadModule passenger_module /usr/lib/ruby/gems/1.8/gems/passenger-2.2.11/ext/apache2/mod_passenger.so
   PassengerRoot /usr/lib/ruby/gems/1.8/gems/passenger-2.2.11
   PassengerRuby /usr/bin/ruby1.8

(apache sites-available stuff)

sudo /etc/init.d/apache2 restart

======== walking papers ===========

% apt-get install curl python-imaging python-numpy openjdk-6-jre-headless
% apt-get install libapache2-mod-php5 php5-gd php5-pgsql php-pear

% pear install Crypt_HMAC HTTP_Request DB
% pear install Crypt_HMAC2 MDB2 MDB2#pgsql

sudo -u postgres -i
  createdb -E UTF8 -O sensory paperwalking

psql -U sensory -h localhost -f site/doc/create.postgres paperwalking

cd wp
git submodule init && git submodule update
cd site && make
cd decoder && make
cd site/lib
cp init.php.txt init.php
nano init.php (change dsn and final_destination)
sudo nano /etc/php5/apache2/php.ini 
  ; Maximum allowed size for uploaded files.
  upload_max_filesize = 25M
