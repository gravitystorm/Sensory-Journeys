<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN"
"http://www.w3.org/TR/html4/strict.dtd"> 
<html>
<head>
  <%= stylesheet_link_tag 'common' %>
  <%= javascript_include_tag :defaults %>
    <% if @title %>
    <title><%=h @title %></title>
    <% else %>
    <title>Admin - Sensory Journeys</title>
    <% end %>
</head>

<body>
 <div class="banner"><%= image_tag "sensory_journeys.png", :alt => 'Sensory Journeys' %>
    <h2>Communal Visualisation of people's experiences while travelling</h2>
    <h2><%= link_to 'ADMINISTRATION INTERFACE', :controller => :admin, :action => :index %></h2>
    <br clear="both"/>
 </div>

    <% if flash[:notice] %>
      <div class="notice"><%=h flash[:notice] %></div>
    <% end %>
    <% if flash[:error] %>
      <div class="error"><%=h flash[:error] %></div>
    <% end %>
  <%= @content_for_layout %>
</body>
</html>